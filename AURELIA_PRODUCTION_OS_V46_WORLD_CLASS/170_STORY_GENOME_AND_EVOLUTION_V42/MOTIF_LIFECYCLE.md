# MOTIF LIFECYCLE

`INTRODUCE → ESTABLISH → VARIATE → DEEPEN → RECONTEXTUALIZE → PAYOFF → REST → RETURN`

A motif may repeat only when its semantic role changes, accumulates meaning, or intentionally provides structural memory.
