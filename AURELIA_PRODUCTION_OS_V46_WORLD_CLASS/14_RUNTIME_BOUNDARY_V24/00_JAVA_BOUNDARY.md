# V24 Java Runtime Boundary

Java is a future implementation target, not a creative authority layer.

Required runtime capabilities: object store, revision store, event store, task orchestrator, context compiler, agent gateway, IR compiler, prompt renderer, model adapter gateway, package store, evidence store, validator, transaction manager, recovery manager and audit store.
