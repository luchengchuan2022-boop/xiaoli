# REPAIR MASTER TASK

Repair flow:
detect defect
→ locate earliest responsible layer
→ classify defect
→ freeze affected output
→ propose minimal patch
→ validate semantic delta
→ authorize
→ apply
→ verify
→ release new revision.

Never overwrite evidence of the defective revision.
