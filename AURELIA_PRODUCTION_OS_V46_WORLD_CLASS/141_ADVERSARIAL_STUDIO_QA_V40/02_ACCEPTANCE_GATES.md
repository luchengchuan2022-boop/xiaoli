# Acceptance Gates

A package is BLOCKED if any hard invariant fails. Soft quality weakness triggers revision, not automatic rejection only when no semantic hard gate is violated.

Release states: BLOCKED, NEEDS_REVIEW, REWORK, ACCEPTED, COMMITTED.
