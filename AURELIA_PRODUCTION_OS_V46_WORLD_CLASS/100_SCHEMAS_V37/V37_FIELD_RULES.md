# V37 FIELD RULES

All contracts require identity, provenance, authority, temporal scope and lifecycle where applicable.

Hard constraints are machine-checkable.
Unknown is not a value; it is a state requiring explicit handling.
Every delta requires before/after semantics and authorization.
Every production artifact requires source references and compiler version.
Every acceptance decision requires evidence references.
