# Semantic Loss Budget

允许的损失只能发生在显式标记为可变的 cinematic expression 层。
不可损失：identity / state delta / relationship delta / world rules / causal order / reveal fairness / required continuity。

每次适配记录 semantic_loss=[]；关键 loss 非空则失败。
