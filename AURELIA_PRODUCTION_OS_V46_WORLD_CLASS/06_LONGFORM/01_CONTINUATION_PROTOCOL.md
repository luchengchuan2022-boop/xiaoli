# CONTINUATION PROTOCOL

"CONTINUE" means:
load latest committed state
→ load unresolved obligations
→ retrieve causal neighborhood
→ retrieve character/relationship context
→ retrieve visual continuity
→ select legal next story space
→ generate candidates
→ evaluate
→ compile
→ validate
→ commit.

It never means "write a fresh episode from memory."
