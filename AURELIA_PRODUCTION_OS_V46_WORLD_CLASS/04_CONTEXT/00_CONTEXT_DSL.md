# CONTEXT DSL

Context is compiled, scoped and frozen.

CONTEXT := AUTHORITY + STATE + TASK + RELEVANCE + OBLIGATIONS + REFERENCES + CONSTRAINTS + HISTORY_WINDOW

A Context Packet declares what was included, excluded, why, and from which revisions.

No hidden memory may be treated as authoritative context.
