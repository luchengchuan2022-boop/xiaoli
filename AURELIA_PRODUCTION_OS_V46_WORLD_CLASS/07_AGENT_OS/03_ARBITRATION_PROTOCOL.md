# AGENT ARBITRATION

When proposals conflict:

detect
→ classify
→ locate authority
→ compare semantic impact
→ evaluate evidence
→ select/merge/escalate
→ validate
→ commit.

If no legal resolution exists: FAIL CLOSED.
