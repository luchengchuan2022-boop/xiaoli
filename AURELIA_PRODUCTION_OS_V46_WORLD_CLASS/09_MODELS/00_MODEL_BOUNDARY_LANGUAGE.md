# MODEL BOUNDARY LANGUAGE

The model is an execution component.

MODEL_INPUT = authorized Prompt AST + approved references + execution policy.

MODEL_OUTPUT = artifact + observation metadata.

Model output cannot directly modify Canon, State, Authority or Policy.
