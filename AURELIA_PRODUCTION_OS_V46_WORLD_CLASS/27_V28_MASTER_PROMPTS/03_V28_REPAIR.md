# V28 REPAIR

Observe defect → classify → trace semantic lineage → find earliest responsible layer → create minimum repair delta → calculate impact → recompile descendants → validate → package → authorize → commit.

Do not rewrite the final prompt merely because the final output is wrong.
