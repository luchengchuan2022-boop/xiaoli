# V24 Replay & Recovery

Every committed state must be reproducible from a valid snapshot plus ordered events, or from an equivalent deterministic rebuild contract.

Recovery paths: retry, replay, rebuild, compensate, abort, quarantine.
