# CANONICAL SERIALIZATION

For hashes, comparisons, signatures, and deterministic builds, serialization must specify:
- field order
- null/missing behavior
- Unicode normalization policy
- numeric representation
- timestamp normalization
- collection ordering
- whitespace policy

Human-readable serialization may differ from canonical serialization.
