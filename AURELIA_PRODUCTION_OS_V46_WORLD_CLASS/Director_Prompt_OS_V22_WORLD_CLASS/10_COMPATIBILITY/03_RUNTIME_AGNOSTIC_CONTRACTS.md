# RUNTIME AGNOSTIC CONTRACTS

CPL/IR/Task Contract 不依赖：
Java / Python / Node / LLM vendor / video vendor。

Runtime 只实现：
load
resolve
validate
compile
execute
capture
transaction
event
snapshot
replay

自然语言 Prompt 不作为 runtime API contract。
