# V23 REPAIR

修复必须从最早责任层开始：
`Canon → State → Knowledge → Story → Dramatic → Cinematic IR → Prompt AST → Renderer → Model Output → Evidence/Package`。

不要从最终 Prompt 层掩盖上游错误。修复前后必须生成 Semantic Delta 并重新验证受影响依赖闭包。
