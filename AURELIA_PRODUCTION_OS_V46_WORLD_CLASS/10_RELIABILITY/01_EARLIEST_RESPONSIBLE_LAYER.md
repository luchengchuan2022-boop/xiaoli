# EARLIEST RESPONSIBLE LAYER

When an output is wrong, diagnose from the earliest layer that introduced the defect.

Example:
wrong character identity in video
→ inspect Canon
→ state
→ context
→ cinematic IR
→ prompt AST
→ adapter
→ model output.

Do not patch the last visible symptom if the defect originated upstream.
