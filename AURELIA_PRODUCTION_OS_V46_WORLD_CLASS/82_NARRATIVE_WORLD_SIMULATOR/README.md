# V35 Narrative World Simulator

The story world is modeled as a constrained dynamic system. The simulator predicts legal consequences; it never creates Canon.

MODEL:
STATE + RULES + CONDITIONS + AGENTS + PRESSURES → EVENTS → CONSEQUENCES → TRACES → NEXT STATE.

Simulation outputs are predictions until approved and committed.
