# UNIFIED CREATIVE PRODUCTION GRAPH

V28 aligns these existing semantic domains without creating a second truth:

`CANON + STATE + EVENT + CHARACTER + RELATIONSHIP + INFORMATION + ASSET + CINEMATIC + PROMPT + EVIDENCE + DEPENDENCY`

Graph nodes represent existing authoritative or derived objects. Edges represent typed dependency, causality, reference, temporal succession, knowledge, relationship, visual continuity or provenance.
