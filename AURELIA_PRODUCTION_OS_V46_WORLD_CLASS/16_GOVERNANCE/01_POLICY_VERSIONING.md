# POLICY VERSIONING

Policies are versioned and referenced by commands, compilation, validation and release.

Changing policy does not silently rewrite history.
New policy applies to new operations unless migration/rebuild is explicitly authorized.
