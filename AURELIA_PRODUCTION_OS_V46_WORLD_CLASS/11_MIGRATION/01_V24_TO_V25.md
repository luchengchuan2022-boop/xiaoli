# V24 → V25 MIGRATION

V24 remains compatible reference material.

Migration sequence:
inventory
→ classify active/reference/deprecated
→ map terminology
→ map object identity
→ map lifecycle
→ map authority
→ validate semantic equivalence
→ emit migration report
→ authorize activation.

If a V24 concept has no V25 destination, preserve it as a compatibility object; do not discard it.
