# REQUIRED RUNTIME CONTRACTS

Future Java implementation must expose contract-level operations for:

`semantic object resolution / context compilation / graph dependency lookup / semantic transformation / cinematic compilation / model capability validation / provenance / impact analysis / repair planning / package closure / evidence closure`

No implementation is approved merely because the UI works.
