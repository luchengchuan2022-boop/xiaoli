# OBSERVABILITY

Production observability must correlate:
task_id
transaction_id
event_id
object_id
revision
package_id
agent_id
compiler_version
adapter_version
model_run_id
validation_id

Logs are evidence of execution, not a substitute for authoritative state.
