# V24 Writeback Master Prompt

Write back only through authorized transactional protocol. Check expected revision, compute semantic delta, validate patch, commit exactly once, emit evidence and receipt. On uncertainty, quarantine rather than guessing.
