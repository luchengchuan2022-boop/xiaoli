# Payoff Debt Ledger

Every meaningful setup creates a tracked obligation: `setup_id`, expected payoff class, urgency, dependency, allowed delay, last touched episode, payoff status.

The planner must prevent both forgotten setups and premature payoffs.

A long-form system should accumulate dramatic tension, not accumulate unresolved garbage.
