# V24 Conflict Protocol

Detect → localize → classify → freeze affected scope → preserve evidence → propose resolution → authorize → apply → revalidate.

Conflict classes include authority, revision, semantic, canon, state, temporal, spatial, identity, reference, asset and capability conflicts.
