# Asset Lineage V35

Every reusable asset has identity, semantic role, source, version, compatibility, transformations, usage history and variation budget.

Reuse is preferred when identity matters. Variation is allowed only inside declared visual and semantic bounds.

A model-generated asset is never canonical merely because it looks consistent.
