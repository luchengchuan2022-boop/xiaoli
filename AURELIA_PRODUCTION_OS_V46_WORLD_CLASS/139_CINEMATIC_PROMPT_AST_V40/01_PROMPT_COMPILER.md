# Prompt Compiler Contract

The compiler must preserve meaning while translating between semantic representation and model-facing language.

It must never:
- invent character motivations;
- invent world rules;
- add relationships;
- alter reveal timing;
- change identity locks;
- convert uncertainty into certainty.

It may add model-operational phrasing when that phrasing does not change semantics.
