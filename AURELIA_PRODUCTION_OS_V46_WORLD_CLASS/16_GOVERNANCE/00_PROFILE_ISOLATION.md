# PROFILE ISOLATION

AURELIA is an active profile, not a hidden global truth.

Every project operation carries profile_id.

Profile data cannot leak across projects unless an explicit shared-library policy authorizes it.
