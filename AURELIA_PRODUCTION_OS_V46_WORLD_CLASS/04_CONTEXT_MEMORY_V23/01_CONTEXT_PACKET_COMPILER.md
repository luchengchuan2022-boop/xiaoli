# V23 Context Packet Compiler

The Context Packet is computed from task scope, dependency closure, temporal neighborhood, character knowledge, active plot obligations, visual references, continuity risks, and profile policy.

The compiler must state what was intentionally excluded when exclusion could affect correctness.

Never substitute a generic summary for missing authoritative context.
