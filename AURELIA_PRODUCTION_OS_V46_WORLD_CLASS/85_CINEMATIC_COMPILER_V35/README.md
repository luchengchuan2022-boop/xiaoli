# V35 Cinematic Compiler

Cinema is compiled from meaning, not decorated onto prose.

SEMANTIC INTENT → AUDIENCE ATTENTION → STAGING → PERFORMANCE → CAMERA → LIGHT → SOUND → KEYFRAME → IMAGE PROMPT → VIDEO PROMPT.

Every cinematic decision requires a reason. If removing the camera trick leaves the meaning unchanged, the trick is probably decoration.
