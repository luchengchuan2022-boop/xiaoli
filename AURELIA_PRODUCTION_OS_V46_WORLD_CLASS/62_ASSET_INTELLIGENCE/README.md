V33 Asset Intelligence OS

Assets become reusable production objects with identity, lineage, compatibility, semantic role, version, usage history, variation budget, and dependency graph.

Reuse is allowed only when semantic and spatial compatibility is proven. Variation must avoid copy-paste fatigue while preserving recognizable identity.

Track asset debt, obsolete versions, unresolved dependencies, and continuity-sensitive assets.

Asset reference -> image prompt -> keyframe -> video prompt must remain traceable.
