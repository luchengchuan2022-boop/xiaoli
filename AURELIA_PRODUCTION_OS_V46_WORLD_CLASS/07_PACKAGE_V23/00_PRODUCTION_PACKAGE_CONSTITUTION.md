# V23 Production Package Constitution

A Production Package is a reproducible dependency graph, not a folder of files.

Minimum graph:
`Task → Context → Decisions → Story/Dramatic IR → Cinematic IR → Prompt AST → Rendered Prompt → Model Job/Observation → Evidence → Validation → Patch/Commit → Release`.

Every artifact is addressable, versioned, traceable, and bound to its inputs.
