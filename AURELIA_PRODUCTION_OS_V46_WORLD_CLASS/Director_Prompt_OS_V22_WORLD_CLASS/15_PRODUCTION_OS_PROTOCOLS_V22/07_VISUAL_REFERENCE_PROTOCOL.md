# V22 Visual Reference Responsibility Protocol

Every reference image has a declared responsibility, for example:
`FACE, HAIR, EYES, COSTUME, SILHOUETTE, POSE, LOCATION, ARCHITECTURE, LIGHTING, PROP, COMPOSITION`

A reference may constrain only its declared responsibility unless the active profile explicitly expands its authority.

This prevents one image from accidentally becoming a total-style overwrite.
