# SEMANTIC LOSS BUDGET

Each compiler stage declares:
protected fields
transformable fields
derived fields
loss budget
fallback behavior

Protected semantics may not be discarded.
If a required field cannot be represented downstream:
CAPABILITY_GAP / COMPILATION_BLOCKED,
not silent deletion.
