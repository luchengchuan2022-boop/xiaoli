# Cinematic Prompt AST V40

Prompts are compiled artifacts, not creative truth.

## AST layers
SemanticIntent → AudienceFocus → DramaticBeat → Performance → Staging → Composition → Shot → Camera → Focus → Lighting → Color → Environment → VFX → Audio → Keyframe → Motion → Constraints.

Every prompt field must be traceable to an upstream semantic reason or explicitly marked as production detail.
