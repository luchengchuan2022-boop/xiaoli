# Java Handoff V40

Java integration remains intentionally deferred. This directory defines contracts only.

The future Java runtime should implement infrastructure around these semantic contracts, not embed creative truth inside Java classes or giant prompts.
