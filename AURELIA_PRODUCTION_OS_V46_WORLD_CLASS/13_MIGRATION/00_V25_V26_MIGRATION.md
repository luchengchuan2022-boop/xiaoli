# V25 → V26 MIGRATION

V25 is preserved as compatibility/reference material.

Migration is:
inventory
→ classify
→ map
→ validate semantic equivalence
→ assign V26 schema
→ generate compatibility projections
→ run golden/regression suite
→ authorize activation.

No V25 document is silently reinterpreted.
