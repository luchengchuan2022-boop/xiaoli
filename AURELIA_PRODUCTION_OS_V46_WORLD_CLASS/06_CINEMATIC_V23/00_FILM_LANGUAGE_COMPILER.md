# V23 Film Language Compiler

The compiler must translate meaning into observable film language:
`attention → staging → performance → composition → lens/scale → camera position → movement → depth → lighting → color → environment → sound → edit rhythm`.

Every major cinematic choice must have a dramatic or experiential reason.
