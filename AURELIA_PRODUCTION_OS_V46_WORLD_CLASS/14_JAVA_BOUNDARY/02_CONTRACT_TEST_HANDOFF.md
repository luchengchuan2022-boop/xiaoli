# CONTRACT TEST HANDOFF

Every Java boundary must receive:
schema fixture
valid case
invalid case
stale revision case
duplicate case
authorization failure
replay case
migration case
observability assertions.

The Runtime is accepted only when it passes the contract suite, not when the UI appears to work.
