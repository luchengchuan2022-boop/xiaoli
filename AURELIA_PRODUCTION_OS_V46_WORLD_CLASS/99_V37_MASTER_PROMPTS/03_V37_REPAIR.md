# V37 REPAIR PROTOCOL

OBSERVE → CLASSIFY → TRACE PROVENANCE → FIND EARLIEST RESPONSIBLE LAYER → DEFINE MINIMUM REPAIR → BLAST-RADIUS ANALYSIS → RECOMPILE → REGRESSION → VALIDATE → EVIDENCE。

修复优先级：
Truth/Authority > State > Causality > Character > Relationship > Audience > Drama > Cinema > Prompt > Model。

如果问题来自人物逻辑，不允许只改 Prompt。
如果问题来自 Canon，不允许只改角色台词。
如果问题来自模型能力，不允许偷偷删掉语义要求。
