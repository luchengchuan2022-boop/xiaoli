# V23 Unified Production IR

The IR is the stable semantic bridge between creative intent and model prompts.

Required families:
`StoryIR → DramaticIR → SequenceIR → SceneIR → BeatIR → ShotIR → PerformanceIR → StagingIR → CameraIR → LightingIR → AudioIR → VFXIR → AssetIR → ContinuityIR → ConstraintIR → DeliveryIR`

Each IR node carries source refs, semantic delta, dependencies, constraints, and compilation provenance.
