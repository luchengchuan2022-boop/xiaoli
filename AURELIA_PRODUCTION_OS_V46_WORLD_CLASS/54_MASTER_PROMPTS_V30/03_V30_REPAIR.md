# V30 REPAIR

OBSERVE → CLASSIFY → TRACE → FIND_EARLIEST_RESPONSIBLE_LAYER → MINIMUM_REPAIR → IMPACT_ANALYSIS → RECOMPILE → VALIDATE → RED_TEAM → PACKAGE

若画面不好但源语义正确，不改 Canon；若语义错误，回到语义层；若状态错误，回到状态层；若模型无法执行，记录 capability gap，不伪造成功。
