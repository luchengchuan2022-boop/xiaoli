# V22 Master Prompt Authority Notice

The V22 master prompts in `16_MASTER_PROMPTS_V22/` are authoritative for V22 execution.

The V20/V21 prompt files retained in this directory are preserved for migration/reference only. They must not be selected as the active runtime prompt when V22 is declared active.

Selection rule:
`V22 active prompt → V22 contracts → active profile → compatible prior reference`
