# PACKAGE BUILD SPEC

Package build:
resolve graph
→ validate revisions
→ compile required views
→ resolve assets
→ render prompts
→ attach model-run metadata
→ run QA
→ collect evidence
→ construct manifest
→ verify closure
→ freeze.

Any missing required node blocks release.
