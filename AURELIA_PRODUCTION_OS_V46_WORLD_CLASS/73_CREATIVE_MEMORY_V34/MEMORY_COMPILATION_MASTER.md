# 最小充分上下文编译器 V34

不要把历史全部塞给模型。

编译：TASK → REQUIRED ENTITIES → DEPENDENCIES → UNRESOLVED CONSEQUENCES → KNOWLEDGE FIREWALL → VISUAL LOCKS → RECENT EVIDENCE → MINIMUM SUFFICIENT CONTEXT.

每条上下文必须标记 authority、revision、provenance、confidence、dependency。

禁止把“模型过去说过的话”自动当成 Canon。
