# V23 Integration Rules

The Java layer must not infer missing creative facts. If the prompt layer reports UNKNOWN, the runtime preserves UNKNOWN.

The runtime must support version pinning, migrations, rollback/recovery, idempotency, optimistic concurrency, auditability, and deterministic semantic transformations.
