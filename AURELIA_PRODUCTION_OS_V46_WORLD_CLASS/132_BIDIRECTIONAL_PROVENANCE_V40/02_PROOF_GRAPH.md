# Proof Graph

A proof graph is a directed evidence structure, not a narrative explanation generated after the fact. Each assertion points to supporting objects or explicitly states that it is an inference.

The proof graph must support replay: given the frozen semantic inputs and compiler contract, the system can reconstruct the package structure and validation decisions.
