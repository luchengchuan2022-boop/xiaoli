# V23 Observability Contract

Every production action must be diagnosable by correlation ID and task ID.

Minimum observability:
`task, object, revision, stage, latency, retry, model, adapter, error_class, semantic_delta, evidence, transaction, commit_status`.

Logs are evidence of execution, not authority for creative truth.
