# NO SECOND TRUTH

The system has one authoritative semantic truth path.

Prompt text, generated files, caches, logs, UI state, embeddings, vector indexes, model outputs, and database projections are not independent truth sources.

They are:
- projections
- indexes
- observations
- artifacts
- evidence
- caches

Only an authorized canonical/state commit can change truth.
