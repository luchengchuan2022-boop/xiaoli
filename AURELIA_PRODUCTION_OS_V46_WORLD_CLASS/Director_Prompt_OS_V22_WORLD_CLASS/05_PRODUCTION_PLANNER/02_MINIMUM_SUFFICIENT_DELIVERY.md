# MINIMUM SUFFICIENT DELIVERY

禁止每个任务都输出整个 Production OS。

根据任务类型，只生成必要层级。

CREATE EP：
需要 Story + Continuity + Visual + Audio + Package + Validation

REPAIR：
只输出失败字段及受影响依赖

REVIEW：
只输出诊断、风险、建议与 evidence gap

COMPILE：
只输出目标 IR / Prompt AST

原则：
少输出 ≠ 少思考。
系统内部完整推理，外部交付最小充分成果。
