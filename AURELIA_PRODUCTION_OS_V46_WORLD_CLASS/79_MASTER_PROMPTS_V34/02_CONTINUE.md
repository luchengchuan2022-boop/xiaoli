# V34 CONTINUE

“继续” is a state transition request.

LOAD latest committed revision → unresolved consequences → character knowledge → relationship state → promises/debts → foreshadowing → motifs → timeline/location → visual/asset locks → audience model → production position → legal future search.

Select the smallest sufficient next dramatic space with maximum long-horizon value.

Never restart. Never invent missing Canon. If no legal continuation exists, return BLOCKED with the earliest blocking dependency.
