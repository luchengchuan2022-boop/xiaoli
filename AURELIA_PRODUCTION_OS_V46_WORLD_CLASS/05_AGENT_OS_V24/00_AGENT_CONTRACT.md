# V24 Agent Contract

Agents are bounded production workers, not independent sources of truth.

Each agent receives a Task Contract + Context Packet and returns `OBSERVATIONS / PROPOSALS / DELTAS / EVIDENCE / UNRESOLVED / OUTPUT_REFS / AUTHORITY_REQUESTS`.
