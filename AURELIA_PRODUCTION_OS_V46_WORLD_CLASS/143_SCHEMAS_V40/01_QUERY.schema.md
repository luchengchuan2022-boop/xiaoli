# Query Schema

```text
GraphQuery {
  query_id,
  intent,
  roots[],
  temporal_scope,
  authority_filter,
  edge_filter[],
  closure_policy,
  confidence_policy,
  context_budget,
  output_schema
}
```
