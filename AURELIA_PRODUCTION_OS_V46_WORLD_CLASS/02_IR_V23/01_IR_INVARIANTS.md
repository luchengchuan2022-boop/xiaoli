# V23 IR Invariants

- IR cannot invent protected story facts.
- Every derived node has traceable upstream meaning.
- Removing an optional renderer field must not silently remove a protected semantic requirement.
- Model-specific features belong in adapters, not the core IR.
- IR versions are explicit and migratable.
