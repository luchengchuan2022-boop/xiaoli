# Proof Schema

```text
ProofBundle {
  assertion_id,
  semantic_inputs[],
  dependency_closure[],
  decision_refs[],
  compiler_trace[],
  observations[],
  validations[],
  hashes[],
  reviewer_refs[],
  replay_contract,
  status
}
```
