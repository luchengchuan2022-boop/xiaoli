# Semantic Preservation Gate

Model output is observation, never authority. When a model cannot realize a requested expression, the system may degrade cinematic detail only after proving protected semantics remain unchanged.

Classes: `SAFE_DEGRADE`, `RECOMPILE`, `HUMAN_REVIEW`, `BLOCK`.
