# V24 Context Integrity

Context packets carry source revisions, snapshot id, retrieval policy, completeness status and stale/conflict markers.

A context packet is invalid if required authoritative dependencies are missing or stale.
