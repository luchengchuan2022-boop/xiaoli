# Semantic Diff Rules

Textually different prompts can be semantically equivalent. Textually similar prompts can be semantically incompatible.

The diff engine must compare canonical fields and graph relationships first, then representation-level fields.

A visual rewrite that changes character intent, knowledge, relationship state, world rule, or audience reveal is a semantic change even if the prose looks like a minor edit.
