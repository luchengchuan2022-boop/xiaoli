# Constraint Satisfaction

Separate:
- HARD constraints: violation = invalid
- SOFT constraints: violation = cost
- PREFERENCE: ranking signal
- UNKNOWN: unresolved, never silently treated as false

The compiler must identify infeasible candidate sets explicitly instead of fabricating a compromise that violates protected truth.
