# Replay Contract

Given the same committed semantic inputs, compiler version, policy version and declared model adapter contract, the system must reproduce the same semantic package structure and decision trace. Pixel-level model output determinism is not claimed.
