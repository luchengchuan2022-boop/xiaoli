# RUNTIME MUST NOT

The Runtime must not:
- become a second Canon
- silently resolve conflicts
- infer missing creative truth as fact
- bypass authorization
- mutate released artifacts
- drop semantic fields because a model adapter cannot support them
- hide transaction indeterminacy
- make legacy documents active by default
