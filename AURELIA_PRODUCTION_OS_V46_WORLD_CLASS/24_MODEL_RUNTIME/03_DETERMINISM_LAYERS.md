# DETERMINISM LAYERS

Distinguish:

`DETERMINISTIC / STABLE / PROBABILISTIC / NONDETERMINISTIC`

Semantic compilation should be reproducible for the same inputs and versions. Model pixels may remain probabilistic. Do not promise deterministic media when the model does not provide it.
