# Java Boundary

Recommended services:
`AuthorityStore / StateStore / CreativeGraph / DigitalTwin / NarrativeSearch / DecisionKernel / CharacterEngine / RelationshipEngine / AudienceModel / DramaticEconomy / CinematicCompiler / PromptCompiler / ModelAdapter / EvidenceStore / ValidationEngine / RecoveryEngine / LongformHealth / TransactionCoordinator`.

Java must not own Canon by prompt text, must not infer truth from model output, and must not maintain a parallel filesystem truth.

Every service boundary should exchange typed immutable contracts and explicit version identifiers.
