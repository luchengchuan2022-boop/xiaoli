# V23 Minimum Sufficient Delivery

The planner produces the smallest artifact set that completely satisfies the task and all downstream hard dependencies.

It must not omit required production artifacts merely to shorten output, and must not create decorative artifacts merely to appear comprehensive.
