# Prompt Renderer Contract

Prompt text is the final renderer, not the source of truth.

Renderer inputs are only approved PromptAST + adapter capability profile + output-format policy.

Renderer MUST preserve protected terms, explicit negatives, timing, reference responsibilities, continuity locks, and performance intent.

Renderer MUST NOT add lore, character facts, plot, or visual identity rules from its own imagination.
