# Reproducibility Contract

Given identical canonical revisions, task contract, compiler version, profile version, adapter version, and deterministic inputs, the semantic IR must rebuild identically.

Model stochasticity belongs to observation generation, not to hidden changes in canonical intent.
