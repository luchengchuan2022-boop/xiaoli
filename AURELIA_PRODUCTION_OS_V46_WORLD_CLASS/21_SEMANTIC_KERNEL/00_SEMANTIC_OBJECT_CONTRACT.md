# SEMANTIC OBJECT CONTRACT

A semantic object must expose:

`SEMANTIC_ID, OBJECT_TYPE, MEANING, AUTHORITY, STATE, CAUSES, CONSEQUENCES, DEPENDENCIES, LINEAGE, STATUS, PROVENANCE, VALIDATION`

Surface wording alone never defines identity.
