# Resource and Asset Graph

Assets are first-class production objects with identity, version, compatibility, reuse potential, cost, risk, and visual responsibility.

Reuse is encouraged only when identity and semantic continuity remain valid.

An asset cannot silently replace another because it is cheaper or easier for a model.
