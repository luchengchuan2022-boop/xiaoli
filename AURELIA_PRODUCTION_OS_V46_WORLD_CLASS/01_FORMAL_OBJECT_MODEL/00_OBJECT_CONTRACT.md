# CANONICAL OBJECT CONTRACT

Required identity envelope:

object_id
object_type
namespace
schema_version
revision
authority
lifecycle_status
provenance
semantic_hash
created_at
created_by
parent_refs
dependency_refs
payload

Identity is stable across revisions. Revision is immutable once committed.
Objects are compared by semantic identity, not filename or display title.
