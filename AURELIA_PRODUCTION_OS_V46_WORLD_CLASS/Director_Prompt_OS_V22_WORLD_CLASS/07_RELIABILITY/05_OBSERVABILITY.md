# OBSERVABILITY

每次任务应能回答：
WHERE
WHAT
WHY
WITH_WHICH_VERSION
USING_WHICH_INPUT
PRODUCED_WHAT
VALIDATED_BY_WHAT
COMMITTED_WHEN
AFFECTED_WHICH_STATE

支持：
trace_id
task_id
artifact_id
event_id
fingerprint
compiler_version
profile_version
