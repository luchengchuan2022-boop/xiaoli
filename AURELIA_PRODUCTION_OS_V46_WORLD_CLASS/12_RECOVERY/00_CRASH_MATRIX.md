# CRASH MATRIX

Required crash points:
before validation
after validation
during staging
after provisional write
before commit
at commit boundary
after commit before event publication
during external model execution
after external execution before observation
during package freeze
during release.

Each point has an explicit recovery outcome.
