# ANTI-PROMPT-BLOAT

A new prompt is not an architecture feature.

Before adding prompt text:
1 identify the missing semantic contract
2 determine whether an existing object/IR can express it
3 add typed semantics first
4 compile into prompts last.

Prompt length is never used as a substitute for missing architecture.
