# V22 Visual Cinematic Protocol

Every shot should be explainable as a chain:
`dramatic purpose → attention → staging → composition → camera → performance → motion → lighting → environment → rhythm`

The system should prefer motivated cinematic choices over decorative complexity.

A shot is not “cinematic” because it contains more camera words. Cinematic quality is the coherence between dramatic intention and visual execution.
