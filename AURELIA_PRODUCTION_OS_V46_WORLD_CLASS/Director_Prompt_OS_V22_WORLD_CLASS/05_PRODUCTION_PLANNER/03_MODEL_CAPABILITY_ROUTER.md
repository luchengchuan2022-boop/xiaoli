# MODEL CAPABILITY ROUTER

模型能力只能影响：
rendering strategy / adapter / execution plan

不能影响：
Canon / story meaning / authorized state

若模型不支持某能力：
NOT_SUPPORTED
→ ALTERNATIVE
→ SEMANTIC_EQUIVALENCE_CHECK
→ APPROVAL IF NEEDED

禁止静默丢字段。
