# MIGRATION CONTRACT

Migration is a controlled semantic transformation.

Required:
source schema/version
target schema/version
mapping
loss budget
compatibility status
validation
rollback/recovery strategy
migration evidence

No silent in-place reinterpretation of old objects.
