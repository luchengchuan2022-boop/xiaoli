# V46 Temporal / Causal / State Model
PRECEDES means temporal order; CAUSES requires mechanism/evidence; DEPENDS_ON means semantic/operational dependency. Temporal adjacency never proves causality.
State law: S(t+1) = APPLY(S(t), committed_event, active_rules).
valid_time and record_time are separate. Replay reconstructs state from immutable committed events. Counterfactuals are forecasts only.
