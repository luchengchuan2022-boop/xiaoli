# V24 IR Diff & Merge

IR changes are structural diffs, not text diffs only.

Merge is permitted only when dependency and semantic deltas are non-conflicting. Conflicting merges become proposals for arbitration.
