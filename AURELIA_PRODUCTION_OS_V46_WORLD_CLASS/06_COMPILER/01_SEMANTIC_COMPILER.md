# SEMANTIC COMPILER

Purpose:
normalize source meaning into structured dramatic/production semantics without inventing protected facts.

Required outputs:
semantic units
entities
actions
states
constraints
dependencies
uncertainties
provenance
